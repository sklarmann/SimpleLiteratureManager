# PDF.js assets

Leg die Dateien `pdf.min.js` und `pdf.worker.min.js` aus dem PDF.js Release (z. B. Version 4.2.67) in dieses Verzeichnis, damit der Viewer ohne externe CDN-Abhängigkeiten funktioniert.
